name: smart_dio
description: A new Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio:
  hive_flutter: ^0.3.1
  path_provider:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
